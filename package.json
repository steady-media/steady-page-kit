{
  "name": "steady-page-kit",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "node --watch server/node.js",
    "start": "node server/node.js",
    "dev:cf": "wrangler pages dev public",
    "deploy:cf": "wrangler pages deploy public --branch main",
    "doctor": "node scripts/doctor.js",
    "check": "sh -c 'for f in functions/*.js functions/posts/*.js functions/rubrik/*.js functions/_lib/*.js functions/api/*.js public/assets/kit-*.js server/*.js scripts/*.js kit.config.js; do [ -e \"$f\" ] || continue; node --check \"$f\" || exit 1; done; echo \"check: alle Dateien ok\"'",
    "test": "node --test test/*.test.js"
  },
  "devDependencies": {
    "wrangler": "^4"
  }
}
